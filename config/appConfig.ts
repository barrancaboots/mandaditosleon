// config/appConfig.ts

/**
 * --------------------------------------------------------------------
 * MODO DE DEPURACIÓN VERBOSE (VERBOSE DEBUG MODE)
 * --------------------------------------------------------------------
 * Poner en `true` para mostrar logs detallados en la consola de desarrollo.
 *
 * IMPORTANTE: Poner en `false` para compilaciones de producción.
 * --------------------------------------------------------------------
 */
export const IS_DEV_MODE = true;